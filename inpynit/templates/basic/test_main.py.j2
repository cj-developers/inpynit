"""
{{ project_name }} 테스트
"""

import pytest
from {{ project_slug }}.main import hello_world


def test_hello_world():
    """hello_world 함수 테스트"""
    result = hello_world()
    assert "Hello from {{ project_name }}" in result
    assert "🎉" in result


def test_hello_world_return_type():
    """hello_world 함수 반환 타입 테스트"""
    result = hello_world()
    assert isinstance(result, str)


def test_hello_world_not_empty():
    """hello_world 함수가 빈 문자열을 반환하지 않는지 테스트"""
    result = hello_world()
    assert len(result) > 0 